# task_manager
A full-stack Task Manager application built with React and Django REST Framework. Features JWT-based authentication, user registration/login, and complete CRUD operations for managing tasks with a clean and responsive UI.
